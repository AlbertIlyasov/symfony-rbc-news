{% extends 'base.html.twig' %}

{% block title %}{{ news.title }} - News{% endblock %}

{% block body %}
<div>
    <h3>{{ news.title }}</h3>
    {% if news.img %}
        {{ news.img }}
    {% endif %}
    {{ news.text | raw }}
</div>
{% endblock %}
